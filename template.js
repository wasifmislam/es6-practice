const firstName = "justin";
const lastName = "timberlake";
const fullName = firstName +" " + lastName + " is a good boy";
const fullName2 = `${firstName} ${20+50+60} is a good man.`

const multiLine = "three lines will be written\n"
       + " sparated from firsy line\n"
       + " is it working"
const multiLine2 = `yes it will work now
I am checking it
yes lines are separated now`
console.log(multiLine2);